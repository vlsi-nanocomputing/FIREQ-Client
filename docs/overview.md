# Overview

FIREQ-Client provides the user-facing Python interface used to configure,
launch, and monitor FIREQ experiments.

The client communicates with FIREQ-Server and exposes high-level functions for
experiment control.
